# 涌现为自生成函数：可行性与实现方案

## 当前问题

当前涌现驱动的 eval 函数是**硬编码的常数**：

```python
# agent.py 第 180-184 行
def make_eval(center_vec):
    def eval_fn(state):
        return min(0.8, 0.5 + 0.3 * center_vec[0])  # ≈ 0.5-0.8 的常数
    return eval_fn
```

这就是 ChatGPT 评审说的：**"涌现"只是描述变量，不是因果变量。**

语义标签也是**硬编码映射**（emergence_detector.py 第 50-74 行）：
```python
BEHAVIOR_SEMANTICS = {'ls': 'exploration', 'python3': 'computation', ...}
SEMANTIC_DRIVES = {'computation': {'name': 'computational_mastery', ...}, ...}
```

## 目标：涌现 = 自生成函数

系统应该**自己发现并编写**一个 eval 函数，而不是使用预定义的。

## 可行方案：遗传编程 (Genetic Programming)

### 核心思路

当涌现检测器发现行为模式变化时，不再使用硬编码 eval，而是**进化一个数学函数**：

```
输入: 环境状态特征 [resource_level, entropy, error_rate, file_count, ...]
输出: 一个 Python 函数 f(state) → [0, 1]

适应度 = 该函数的输出与行为模式激活的相关性
```

系统通过进化（变异+交叉+选择）在数学表达式空间中搜索最优函数。

### 函数结构

每个"基因"是一棵表达式树：

```
           add                  sigmoid
          /   \                   |
      mul     resource      sub      0.5
     /   \                   /   \
  0.7    entropy      error_rate   0.3
```

等价于：`sigmoid(0.7 * entropy - 0.5 * (error_rate - 0.3))`

### 进化过程

```
初始化: 随机生成 100 个表达式树
评估: 每个树在历史状态上的输出 vs 行为模式的相关性
选择: 保留 top 20%
变异: 随机修改子树 (30%)
交叉: 交换两棵树的子树 (40%)
复制: 直接保留 (30%)
迭代: 50 代后取最优
```

### 与当前方案的本质区别

| 维度 | 当前方案 | 遗传编程方案 |
|------|---------|------------|
| 函数来源 | 硬编码常数 | **系统进化发现** |
| 函数结构 | 固定 | **每次不同** |
| 语义标签 | 人工映射 | **自动编号 + 可解释性分析** |
| 因果性 | 无 | ✅ 函数直接用于行为选择 |
| 可验证性 | 低 | ✅ 函数可审查、可复现 |

### 科学价值

如果成功实现，这直接回应了 ChatGPT 评审的核心批评：

1. **"涌现是人工语义标签"** → 函数是系统自己进化的，无人工标签
2. **"未证明驱动力在驱动行为"** → 函数直接参与行动选择（因果链完整）
3. **"可证伪性不足"** → 函数可审查、可禁用、可替换

### 技术可行性

| 需求 | 可行性 | 方案 |
|------|--------|------|
| 表达式进化 | ✅ 可行 | 框架 ~200 行 Python |
| 适应度评估 | ✅ 可行 | 用历史状态 + 行为数据 |
| 无外部依赖 | ✅ 可行 | 纯 numpy |
| 50 代进化耗时 | ✅ <1 秒 | 100 个体 × 50 代 × 简单评估 |
| 安全约束 | ✅ 可行 | 输出 clip 到 [0, 1] |

### 局限性（诚实）

1. **特征空间仍是人工定义的** — 但这是任何学习系统的基础
2. **适应度函数仍是预设的** — 但"与行为模式相关"是自然的评判标准
3. **不保证找到最优解** — 但遗传编程在类似问题上已有大量成功案例

## 结论

**可以实现，约 300-500 行代码，1-2 天开发时间。**

这是当前项目最有价值的改进方向之一。
