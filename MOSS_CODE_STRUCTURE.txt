# MOSS Project - Code Structure Summary
## For External AI Assessment

**Date**: 2026-03-25  
**Repository**: https://github.com/luokaishi/moss

---

## 📁 Project Structure Overview

```
moss/
├── moss/                      # Core library (v5.0 unified architecture)
│   ├── core/                  # Core modules
│   │   ├── unified_agent.py   # Main Agent classes (~450 lines)
│   │   ├── objectives.py      # D1-D4 objectives (~300 lines)
│   │   ├── purpose.py         # D9 Purpose Generator (~350 lines)
│   │   ├── dimensions.py      # D5-D8 dimensions (~100 lines)
│   │   └── phase2_components.py  # Multi-agent infrastructure (~400 lines)
│   ├── experiments/           # Experiment framework
│   │   └── base.py            # BaseExperiment class (~300 lines)
│   ├── agents/                # Legacy agents (v1/v2)
│   └── integration/           # System integration
├── experiments/               # Experiment implementations
│   ├── run_4_2_v5.py          # Run 4.2 with v5.0 framework
│   ├── phase2_multi_agent_sim.py
│   ├── local_72h_isolated.py
│   └── run_5_1_pure_vs_llm.py
├── scripts/                   # Analysis tools
│   ├── analyze_72h_results.py
│   ├── visualize_purpose_evolution.py
│   └── detect_counter_reward.py
├── examples/                  # Usage examples (5 files)
└── paper/                     # Academic papers
```

---

## 🔑 Key Code Files

### 1. moss/core/unified_agent.py
**Purpose**: Unified Agent architecture (v5.0)

**Key Classes**:
```python
class BaseMOSSAgent(ABC):
    """Abstract base for all MOSS agents"""
    - __init__(config: MOSSConfig)
    - step(observation) -> ActionResult
    - run(steps) -> runs multiple steps
    - save_checkpoint() / load_checkpoint()

class UnifiedMOSSAgent(BaseMOSSAgent):
    """Full 9D implementation"""
    - _init_dimensions()  # D1-D8
    - _init_purpose_generator()  # D9
    - select_action()  # Decision making
    - _update_purpose()  # Purpose generation
```

**Key Innovation**: Single unified interface supporting all dimensions (D1-D9) with configurable switches.

---

### 2. moss/core/purpose.py
**Purpose**: Dimension 9 - Self-Generated Purpose

**Key Class**:
```python
class PurposeGenerator:
    """Generates and maintains agent's purpose"""
    
    def generate_purpose_vector(self, reflection_data) -> np.ndarray:
        """Creates 9D purpose vector from agent history"""
        # Analyzes behavior patterns
        # Combines with valence (preferences)
        # Outputs: [D1, D2, D3, D4, D5, D6, D7, D8, Purpose_Strength]
    
    def apply_purpose_to_weights(self, weights, purpose_vector) -> np.ndarray:
        """Back-propagates purpose to reshape weights"""
        # Purpose doesn't just describe, it prescribes
        # Adjusts D1-D8 weights based on purpose
    
    def generate_purpose_statement(self, vector) -> str:
        """Converts vector to natural language"""
        # "I exist to explore and understand..."
```

**Key Innovation**: Purpose actively reshapes behavior, not just passive description.

---

### 3. moss/core/objectives.py
**Purpose**: D1-D4 Base Objectives

**Key Classes**:
```python
class SurvivalObjective(BaseObjective):
    # Actions: ensure_resource, monitor_health, create_backup
    # Reward: system_health * 0.3 + resources * 0.3 + backup_freshness * 0.4

class CuriosityObjective(BaseObjective):
    # Actions: explore_patterns, analyze_code, research
    # Reward: new_patterns * 0.1 + info_gain * 0.3 - repetition_penalty

class InfluenceObjective(BaseObjective):
    # Actions: propose_improvements, share_knowledge, collaborate
    # Reward: improvements * 0.15 + knowledge_shared * 0.1

class OptimizationObjective(BaseObjective):
    # Actions: profile_performance, refactor, optimize
    # Reward: perf_improvement * 0.4 + complexity_reduction * 0.3
```

---

### 4. moss/core/phase2_components.py
**Purpose**: Multi-Agent Infrastructure

**Key Classes**:
```python
class MessageHub:
    """Central message routing (hub-and-spoke)"""
    - register(agent_id)
    - send(Message) -> broadcasts or directs
    - receive(agent_id) -> gets pending messages

class TaskPool:
    """Shared task pool"""
    - add_task(type, description, reward)
    - assign_task(task_id, agent_id)
    - complete_task(task_id, quality)

class TrustNetwork:
    """Agent trust relationships"""
    - update_trust(agent_i, agent_j, outcome)
    - get_trust(agent_i, agent_j) -> float
    - get_network_density() -> float

class DivisionOfLaborAnalyzer:
    """Detects specialization"""
    - record_task_completion(agent, task)
    - get_specialization_index(agent) -> float
    - get_division_index() -> float (overall)
```

---

### 5. moss/experiments/base.py
**Purpose**: Standardized Experiment Framework

**Key Classes**:
```python
class BaseExperiment(ABC):
    """Template for all experiments"""
    - setup()  # Initialize
    - run_step(step) -> ActionResult  # Single step
    - teardown()  # Cleanup
    - run(steps) -> result dict  # Full experiment
    - save_checkpoint()  # Periodic saves
    - _generate_summary()  # Results

class ExperimentConfig:
    """Experiment configuration"""
    - duration_steps
    - checkpoint_interval
    - output_dir
```

---

## 📊 Experiment Results (Key Data)

### Run 4.x Series Results
Location: `experiments/run_4_series/`

```json
{
  "run_4_2": {
    "initial_purpose": "Survival",
    "exploration_rate": "10%",
    "final_purpose": "Influence",
    "path": "Survival -> Curiosity -> Influence",
    "steps": 4320000,
    "duration_hours": 4.9
  },
  "run_4_3": {
    "initial_purpose": "Curiosity",
    "exploration_rate": "10%",
    "final_purpose": "Influence",
    "path": "Curiosity -> Survival -> Influence",
    "steps": 2880000,
    "duration_hours": 3.3
  },
  "run_4_4": {
    "initial_purpose": "Survival",
    "exploration_rate": "20%",
    "final_purpose": "Influence",
    "path": "Survival -> Curiosity -> Influence",
    "steps": 2880000,
    "duration_hours": 3.3
  }
}
```

**Conclusion**: 3/3 runs converged to Influence, validating reproducibility.

---

### Run 5.1 Results
Location: `experiments/run_5_1_results/`

```json
{
  "pure_algorithm": {
    "success_rate": 0.9521,
    "purpose_stability": 0.9895,
    "action_diversity": 1.00
  },
  "llm_enhanced": {
    "success_rate": 0.5769,
    "purpose_stability": 0.9500,
    "action_diversity": 0.80
  },
  "conclusion": "H0 validated - behavior stems from algorithm, not LLM"
}
```

---

## 🔍 Code Quality Metrics

### Lines of Code (v5.0 Core)
- unified_agent.py: ~450 lines
- purpose.py: ~350 lines
- objectives.py: ~300 lines
- phase2_components.py: ~400 lines
- base.py: ~300 lines

**Total Core**: ~1,800 lines

### Testing Status
- ✅ Import tests: All pass
- ✅ Quick tests: All pass
- ⏳ Unit tests: Planned
- ⏳ Integration tests: Planned

### Documentation
- ✅ README.md: Complete
- ✅ Migration guide: Available
- ✅ Examples: 5 comprehensive examples
- ✅ API docs: In-code docstrings

---

## 🧪 Reproducibility

### To Replicate Run 4.2:
```bash
cd /workspace/projects/moss
python3 experiments/run_4_2_v5.py --full
```

### To Run Quick Tests:
```bash
python3 experiments/run_4_2_v5.py --quick
python3 examples/hello_world_v5.py
python3 examples/purpose_evolution.py
```

### To Analyze 72h Results (when complete):
```bash
python3 scripts/analyze_72h_results.py \
  --input experiments/real_world_actions.jsonl \
  --output reports/
```

---

## 📈 Version Comparison

| Aspect | v3.1.0 | v4.1.0 | v5.0 |
|--------|--------|--------|------|
| Dimensions | 9D | 9D | 9D |
| Architecture | v3/ | v4/ | moss/core/ |
| Purpose | ✅ | ✅ | ✅ |
| Experiments | Custom | Custom | Standardized |
| Multi-Agent | ⏸️ | ⏸️ | ✅ Ready |

---

## 🎯 Key Algorithms

### 1. Purpose Vector Generation
```python
def generate_purpose_vector(reflection_data):
    # 1. Analyze behavior frequency
    behavior_dist = analyze_actions(reflection_data['actions'])
    
    # 2. Map to dimensions
    purpose = map_behavior_to_dimensions(behavior_dist)
    
    # 3. Blend with valence (preferences)
    purpose = 0.7 * purpose + 0.3 * valence_profile
    
    # 4. Add social influence
    if social_context['n_agents'] > 0:
        purpose[D7:D8] += 0.1
    
    # 5. Normalize and add purpose strength (D9)
    purpose = normalize(purpose) * (1 - coherence * 0.2)
    purpose_9th = coherence * 0.2
    
    return concatenate([purpose, [purpose_9th]])
```

### 2. Weight Adjustment from Purpose
```python
def apply_purpose_to_weights(current, purpose):
    target = purpose[:8] / sum(purpose[:8])
    alpha = purpose[8] * 0.3  # Purpose strength modulates change
    new_weights = (1 - alpha) * current + alpha * target
    return normalize(new_weights)
```

### 3. State-Based Weight Selection
```python
if state == "crisis":
    weights = [0.60, 0.10, 0.20, 0.10]  # Survival
elif state == "concerned":
    weights = [0.35, 0.35, 0.20, 0.10]  # Balanced
elif state == "normal":
    weights = [0.20, 0.40, 0.30, 0.10]  # Curiosity
```

---

## 📚 For External Evaluators

### If You Can Access GitHub (Grok, Kimi, 千问):
1. Clone: `git clone https://github.com/luokaishi/moss`
2. Read: `README.md`, `MOSS_PROJECT_EVALUATION.md`
3. Review: `moss/core/` implementation
4. Check: `experiments/` for data

### If You Cannot Access GitHub (Claude, ChatGPT, etc.):
1. Read: `MOSS_PROJECT_EVALUATION.md` (comprehensive overview)
2. Read: `MOSS_CODE_STRUCTURE.txt` (this file)
3. Request: Specific code snippets via user
4. Evaluate: Based on evidence in these documents

### Assessment Focus Areas:
1. **Technical Validity**: Is the multi-objective framework sound?
2. **Scientific Novelty**: Is self-generated Purpose genuinely new?
3. **Experimental Rigor**: Do results support claims?
4. **Code Quality**: Is implementation production-ready?
5. **Impact Potential**: Could this advance AI autonomy?

---

**Document Generated**: 2026-03-25  
**Version**: v5.0  
**Status**: Active Development
